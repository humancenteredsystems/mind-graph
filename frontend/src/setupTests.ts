// Import Vitest's expect and extend it with jest-dom matchers

import '@testing-library/jest-dom/vitest';

// You can add other global test setup here if needed
// For example, mocking global functions or setting up mocks

console.log('Vitest setup file loaded: Extending expect with jest-dom matchers.');
