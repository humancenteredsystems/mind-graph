/**
 * Layout Hook - Provides access to layout context
 * 
 * Re-exports the useLayoutContext hook for convenience and consistency
 * with the existing hook naming pattern in the application.
 */

export { useLayoutContext as useLayout } from '../context/LayoutContext';
