# Environment variables for local development
# See docs/setup-guide.md for detailed information about the centralized configuration system

# API Server Configuration
PORT=3000                            # Port for the Express API server

# Dgraph Configuration
DGRAPH_BASE_URL=http://localhost:8080      # Dgraph base URL (GraphQL and admin URLs are computed from this)

# Admin API Authentication
ADMIN_API_KEY=your_secure_key_here   # Secret key for admin-protected endpoints

# Multi-Tenant Configuration
ENABLE_MULTI_TENANT=true             # Enable multi-tenant functionality (true/false)
DGRAPH_NAMESPACE_DEFAULT=0x0         # Default/system namespace
DGRAPH_NAMESPACE_TEST=0x1            # Test tenant namespace for development
DGRAPH_NAMESPACE_PREFIX=0x           # Prefix for generated tenant namespaces

# CORS Configuration
CORS_ORIGIN=*                           # Allowed CORS origin (* for all, or specific domain like https://yourdomain.com)

# Development Notes:
# - All URLs (GraphQL, admin, alter) are computed from DGRAPH_BASE_URL in api/config/index.js
# - CORS origins are handled in the server configuration
# - See docs/setup-guide.md for adding new configuration options
# - Use the centralized config module instead of direct process.env access
