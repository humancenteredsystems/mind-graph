# Environment variables for local development

# API Server Configuration
PORT=3000                            # Port for the Express API server
CORS_ORIGIN=http://localhost:5173    # Allowed CORS origin for frontend during development

# Local Dgraph Configuration
DGRAPH_URL=http://localhost:8080/graphql      # Dgraph GraphQL endpoint
DGRAPH_ADMIN_URL=http://localhost:8080/admin/schema  # Dgraph Admin Schema endpoint

# Admin API Authentication
ADMIN_API_KEY=your_secure_key_here   # Secret key for /api/admin endpoints

# Remote Dgraph SSH Access (for schema push)
DGRAPH_SSH_HOST=ssh.virginia.render.com    # Remote SSH host 
DGRAPH_SSH_USER=srv-abc123                 # Remote SSH username (from Render dashboard)
DGRAPH_SSH_KEY_PATH=/path/to/private_key   # Path to SSH private key file
# DGRAPH_SSH_KEY=-----BEGIN RSA PRIVATE KEY-----\n... # Alternative: direct key content
